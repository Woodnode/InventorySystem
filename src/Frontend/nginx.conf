server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # React Router (client-side routing) : toute route inconnue retombe sur index.html
    # plutôt qu'un 404 nginx.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
